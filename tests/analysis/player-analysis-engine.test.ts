// TODO: Wire up Jest test runner and cover PlayerAnalysisEngine factor weighting.
// Example outline:
// import { PlayerAnalysisEngine } from "@/lib/analysis";
// describe("PlayerAnalysisEngine", () => {
//   it("produces a composite score within range", async () => {
//     const engine = new PlayerAnalysisEngine(mockDataService as any);
//     const result = await engine.analyzePlayer({ ... });
//     expect(result.compositeScore).toBeGreaterThanOrEqual(0);
//     expect(result.compositeScore).toBeLessThanOrEqual(1);
//   });
// });
